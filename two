Outputs:
  EDDSecurityGroupId:
    Value: !GetAtt EDDSG.GroupId
    Description: The security group Id of the EDD servlet
    Export:
      Name: !Sub 'a${AssetInsightId}-${ProductName}-${ServiceEnvironment}-${AWS::Region}'
